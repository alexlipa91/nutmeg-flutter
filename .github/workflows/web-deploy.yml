name: Web Build and Deploy

on:
  push:
    branches:
      - master
    paths:
      - 'flutter/**'

jobs:
  deploy_web:
    name: Deploy Web
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: flutter
    steps:
      # --- common: checkout ---
      - name: Checkout code from ref
        uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}
      # --- common: env file ---
      - name: Create env file
        run: |
          echo "${{ secrets.ENV_PROD }}" > .env.prod
          echo "COMMIT_TIMESTAMP=$(git log -1 --format=%ct)" >> .env.prod
      # --- common: flutter setup ---
      - name: Run Flutter tasks
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.0"

      # --- web: production ---
      - name: Build web app
        run: flutter build web --dart-define-from-file=.env.prod --release
      - name: Deploy to Firebase (production)
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting:live
        env:
          GCP_SA_KEY: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_NUTMEG_9099C }}"
          PROJECT_PATH: flutter

      # --- web: test channel ---
      - name: Create test env file
        run: |
          grep -v '^TEST_MODE=' .env.prod > .env.test
          echo "TEST_MODE=true" >> .env.test
          echo "LOG_LEVEL=INFO" >> .env.test
      - name: Clean before test build
        run: rm -rf build/web
      - run: ENV_FILE=.env.test ./scripts/build_web_app.sh

      - name: Deploy to Firebase (test channel)
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting:staging
        env:
          GCP_SA_KEY: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_NUTMEG_9099C }}"
          PROJECT_PATH: flutter
